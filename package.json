{
  "name": "oif-specs",
  "version": "0.1.0",
  "description": "OIF protocol standards and specifications",
  "scripts": {
    "generate:openapi": "ts-node scripts/generate-openapi.ts",
    "build": "npm run generate:openapi"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0",
    "typescript-json-schema": "^0.63.0",
    "js-yaml": "^4.1.0",
    "@types/js-yaml": "^4.0.5"
  }
}